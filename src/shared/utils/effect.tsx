import { Effect } from "../types/effect";

const effectAction = (type: Effect) => {
    switch(type) {
        case Effect.DAMAGE:
            break;
        case Effect.HEAL:
            break;       
    }
};

